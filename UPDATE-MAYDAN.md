# ملفات ميدان 1.3.1 — الغرف الجماعية

تتضمن هذه النسخة الهوية الصلصالية المعتمدة، وتضيف تجربة «مين فينا؟» من **3–12 هاتفًا**: إنشاء غرفة، رمز ورابط وQR للدخول، اختيار الشخصية والجاهزية، تصويت سري، ونقاط ونتائج مشتركة. يحفظ الخادم المقعد بعد الانقطاع وينقل الإدارة عند غياب المضيف.

## الحزمة المناسبة

- **Maydan-update-1.3.1.zip**: الملفات الجديدة والمعدّلة، وتشمل تحديث الجرافيك 1.2.0 والغرف 1.3.1 معًا. انسخ محتويات مجلد الحزمة إلى جذر مستودع Maydan الموجود، واستبدل الملفات المطابقة مع الحفاظ على مساراتها.
- **Maydan-full-project-1.3.1.zip**: المصدر الكامل مع بنك الأسئلة والوسائط. استخدمه إذا لم تكن لديك نسخة من المشروع على الكمبيوتر.

الحزمتان لا تتضمنان `node_modules` أو ملفات الدخول. `SHA256SUMS` مرفق لفحص سلامة الملفات. لا تضع مجلد الحزمة كطبقة إضافية داخل المستودع؛ يجب أن يكون `package.json` بجانب `src` و`public` مباشرة. لا ترفع ZIP إلى المستودع باعتباره تحديثًا للكود.

إذا كنت قد ضبطت عنوان خادمك في `online.config.json` للخيار المنفصل، احتفظ بإعداداته عند دمج التحديث.

حزمة التحديث مبنية على النسخة `ca8e74a7473010d374ac22270a88bb3d9ed6a8e5` من `mf103871-boop/Maydan`. إذا غيّرت الملفات نفسها بعد هذه النسخة، ادمج التغييرات عند النسخ.

## رفع اللعبة والغرف معًا على Cloudflare

اتبع `docs/CLOUDFLARE.md`. من جذر المشروع، باستخدام Node.js 22 أو أحدث:

```bash
npm ci
npx wrangler login
npm run cloudflare:deploy
```

الأمر الأخير ينشر اللعبة والوسائط والخادم على رابط واحد عندما تشغّله بنفسك. لا تحتاج تعديل `online.config.json` لهذا الخيار، أو تثبيت إضافة في ChatGPT. لتفادي تحديث مشروع آخر يحمل الاسم نفسه، راجع اسم `maydan-game` في `wrangler.jsonc` قبل النشر. ويمكن إكمال النشر من متصفح الهاتف بربط فرع `main` عبر Workers Builds؛ الإعدادات الدقيقة في `docs/CLOUDFLARE.md`.

## تشغيل خادم الغرف منفصلًا عن الواجهة

تحتاج Node.js 22 أو أحدث وحساب Cloudflare على Workers Free. من مجلد المشروع:

```bash
npm ci
npx wrangler login
npm run rooms:deploy
```

الأمر الأخير **ينشر خادم الغرف عندما تشغّله بنفسك**. بعده انسخ رابط Worker الذي يظهر إلى قيمة `serverUrl` في `online.config.json`، ثم ارفع تحديث المشروع بنفسك إلى GitHub. سير العمل الموجود أصلًا ينشر الواجهة عند الرفع إلى `main`.

الدليل الكامل في **docs/ONLINE.md**، ويشرح التفعيل، والتجربة المحلية دون نشر، وحدود المجاني، والتعامل مع الانقطاع. رفع الواجهة وحدها لا يكفي؛ إبقاء عنوان الخادم فارغًا يبقي الغرف غير مفعّلة.

## حالة التحقق

اجتازت النسخة **150 اختبارًا**، ونجح بناء الموقع وفحص حزمة Wrangler دون نشر. تشمل الاختبارات مباراة بخمسة أدوار وثلاثة اتصالات داخل محرك Cloudflare المحلي مع SQLite، وإسبات الغرفة ثم استئنافها. لم يكتمل فحص بصري على هواتف فعلية أو اختبار Cloudflare المنشور؛ هذه الخطوة تأتي بعد تفعيل حسابك ورفع النسخة.

الغرف الحالية لمتصفحات الهواتف وPWA عبر HTTPS. الغلاف الأصلي في مجلد iOS يحتاج ربطًا واختبارًا منفصلين قبل استخدام الغرف داخله. بقية الألعاب متاحة على جهاز واحد.

حزمة الملفات الأصلية جُهّزت دون نشر. ربط فرع `main` بحساب Cloudflare يتيح إكمال النشر؛ لا يُعد وجود الملفات على GitHub تأكيدًا لنجاح نشر اللعبة، ويجب التحقق من رابط Worker بعد اكتمال البناء. (الفرع `cloudflare-ready` كان مسارًا مؤقتًا للإصدار 1.3.1 وهو مجمَّد عنده؛ النشر اليوم من `main` كما في `README.md` و`docs/CLOUDFLARE.md` و`docs/ONLINE.md`.)
