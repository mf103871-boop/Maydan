# إعادة بناء محتوى فبركة — 18 سبتمبر 2026

استُبدل البنك النشط السابق كله (154 حقيقة، و12 صورة، و24 موقف أصحاب) ببنك جديد من 180 بطاقة:

- 100 حقيقة موزعة بالتساوي على 10 مواضيع، مع مصدر أولي وشرح وإجابات بديلة مقبولة وفبركات مقترحة لكل حقيقة.
- 32 سؤال صور عن أدوات حقيقية، برسوم مولدة خصيصًا ومراجعة بصريًا، ومصادر مستقلة تشرح وظيفة الأداة.
- 48 موقفًا جديدًا عن الأصحاب، بصياغة تخيلية محايدة و8 اقتراحات لكل موقف؛ يحدد اللاعب الحقيقة في هذا النمط.

أزيلت الأسئلة والرسوم القديمة من البنك النشط. يحتفظ Git بتاريخها. لا تُستأنف الجلسات المحلية غير المنتهية التي تحمل البنك القديم، مع رسالة توضح السبب؛ تبقى النتائج المكتملة قابلة للعرض. لا يجري مسح بيانات اللاعبين أو إغلاق الغرف المنشورة من هذه التغييرات. عند نشرها، ينبغي تحديث العميل وبدء غرفة جديدة للحصول على البنك الجديد وبروتوكول فبركة 3.

## الصور

الملفات الفعلية موجودة في `public/media/fabraka-v3/`: عددها 32، بدقة 1254 × 1254، وصيغة WebP، وحجم إجمالي 3,203,428 بايت. يضم `generated-art.json` وصف توليد كل صورة وبصمتها وحجمها. حُفظت نسخ PNG الأصلية ومعرض مراجعة مستقل ضمن مخرجات جلسة العمل المحلية.

هذه رسوم توضيحية مولدة لأدوات حقيقية، وليست صورًا توثيقية للقطع التي تصفها المصادر. يظهر هذا الإفصاح داخل اللعبة. الصورة 021 هي فرجار التناسب؛ استُبعدت محاولتا رسم بانتوغراف غير دقيقتين من الأصول النهائية.

يمكن تكبير الصورة، وتظهر حالة تحميل وإعادة محاولة عند الفشل. تتحقق عملية البناء من وجود جميع الصور قبل استبدال البناء السابق. تُنسخ الصور إلى حزمة الويب وموارد iOS، وتُخزّن للاستعمال دون اتصال بعد تثبيت العامل الخدمي بنجاح. تغيير الصورة يغير بصمة عنوانها والكاش تلقائيًا.

ترسل لقطة الغرفة وصف الصورة ومسارها المحايدين، ولا ترسل الإجابة أو المصدر قبل الكشف. هذا لا يمنع فحص البنك المضمّن في ملفات اللعب المحلي.

## المراجعة والتحقق

- مجموعة اختبارات المشروع: 447 اختبارًا ناجحًا.
- اختبارات متصفح مستقلة للصور: 6 ناجحة، تشمل التحميل والفشل وإعادة المحاولة والتكبير ولوحة المفاتيح وعدم الاتصال وإصلاح الكاش.
- فحص البنك والأصول بعد تصحيح الصورة 021: 5 اختبارات ناجحة؛ أُعيد على المحتوى النهائي.
- تجربة عبر واجهة اللعبة في Chrome بمقاس 390 × 844: ثلاث جولات صور كاملة لثلاثة لاعبين، وتكبير، وتصويت، وكشف، ونتائج، ثم إعادة تحميل واسترجاع النتيجة؛ نجحت 15 نقطة تحقق دون أخطاء JavaScript أو تجاوز عرض الشاشة.
- معرض المراجعة: تحميل الصور الـ32، وتصفية 100 حقيقة و48 موقفًا، وعرض 180 بطاقة إجمالًا؛ فُحص على الجوال وسطح المكتب.
- نجح بناء Cloudflare وتجهيز موارد iOS. تجهيز الموارد لا يعادل تجميع Xcode أو اختبار جهاز iPhone حقيقي.

تفاصيل مصادر الحقائق ومقارنة البنك السابق في `facts-review.md`، ومصادر الأدوات وملاحظات مراجعة الصور في `pictures-research.md`. راجع مدقق آخر عينة من عشر حقائق ولم يجد خطأ واقعيًا؛ حُسّنت بعض روابط المصادر القديمة. لا تمثل هذه العينة ضمانًا لتحقق مستقل شامل لكل معلومة.

هذا التغيير مُعد للمراجعة على فرع مستقل؛ لا يتضمن نشرًا أو دمجًا في الفرع الرئيسي.
