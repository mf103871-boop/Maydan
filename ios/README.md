# تحديث ميدان على iOS خارج Replit

هذا مشروع UIKit وWKWebView مستعاد من الحزمة المحفوظة `maydan-mobile-beta-0.9.2.zip` (نسخة 2026-09-01). يشغّل اللعبة والوسائط من داخل التطبيق. استُبدلت هوية النسخة التجريبية بهوية التطبيق المنشور المؤكدة من Apple وصورة المستخدم: Bundle ID هو `Maydan` وApple ID هو `6808385717`، والإصدار المنشور `1.0`. فريق المطور `96WJBK2MB2` مؤكد من مستند Apple Developer الذي قدّمه المستخدم سابقًا.

جُهّز الإصدار `1.1` بالبناء المبدئي `1`؛ يجب التأكد من عدم استخدام هذا البناء في إصدار TestFlight غير منشور قبل الرفع. طلب المستخدم هوية جديدة: اسم المتجر المقترح «ميدان: ألعاب جمعتنا» والاسم تحت أيقونة الهاتف «ميدان». الأيقونة الجديدة بدقة 1024 بكسل، وتُولّد المقاسات الصغيرة منها أثناء التجهيز. هذه تغييرات في المشروع، وتحتاج تحديث بيانات المتجر ورفع بناء موقّع لتظهر في App Store. تفاصيل الهوية في `docs/branding.md`.

الحد الأدنى iOS 16.4 يطابق الإصدار المنشور. يستهدف هذا التحديث iPhone، مثل بيانات المتجر الحالية التي لا تعرض ميزة iosUniversal أو صور iPad؛ دعم iPad الأصلي المنفصل يبقى خارج هذا التحديث. مصادر المطابقة في `ios/app-store.json`.

## المحتوى والبناء

شغّل `npm ci` ثم `npm run ios:prepare` من جذر المستودع. الأمر الأخير يبني محتوى الفرع الحالي، وينسخ جميع ملفات اللعبة والوسائط إلى حزمة iOS، ويتحقق من تطابق كل ملف. المجلد الناتج غير متتبّع في Git، لذلك يجب تشغيل الأمر قبل كل بناء في Xcode.

افتح `ios/Maydan.xcodeproj` واختر مخطط `Maydan`. لا يعتمد المشروع على Replit أو CocoaPods أو Expo. محفوظات اللعبة تستخدم مخزن WebKit الدائم في هذا الغلاف، لكن انتقال المحفوظات من تطبيق Replit المنشور لا يمكن ضمانه قبل معرفة غلافه وأصل تخزينه واختبار التحديث فوقه.

يشغّل `iOS build` على GitHub فحوص اللعبة ثم يبني تطبيقًا لمحاكي iPhone باستخدام Xcode 26 أو أحدث. الملف الناتج مخصص للمحاكي، ولا يمكن تثبيته على هاتف حقيقي أو إرساله إلى App Store. هذا يثبت قابلية الترجمة فقط، ولا يغني عن تجربة التطبيق على جهاز حقيقي.

## قبل إنشاء تحديث المتجر

1. استخدم سجل التطبيق الحالي `6808385717`؛ لا تنشئ سجل تطبيق جديد. المعرّف والفريق والإصدار العام صارت مؤكدة؛ راجع فقط أي بناء تجريبي غير منشور عند تحديد رقم البناء النهائي.
2. جهّز شهادة التوقيع وملف provisioning اللذين يطابقان `96WJBK2MB2` و`Maydan`، وبيانات الرفع الآمن إلى App Store Connect.
3. افتح المشروع في Xcode 26 أو أحدث، وحدّد فريق التوقيع، وابنِ Archive، ثم اختبر النسخة عبر TestFlight، خصوصًا التحديث فوق التطبيق القديم والحفظ والوسائط والحركة والاتجاهين.
4. ارفع البناء إلى سجل التطبيق الحالي، وأنشئ إصدارًا مطابقًا له في App Store Connect. طبّق الاسم والعنوان الفرعي ونص «ما الجديد» من `ios/app-store.json` على الترجمة المناسبة، ثم اختر البناء وأرسل الإصدار لمراجعة Apple. تغيير ملف JSON وحده لا يحدّث صفحة المتجر.

يمكن تشغيل التوقيع والرفع أيضًا على macOS في GitHub Actions بعد تجهيز شهادة التوزيع وملف provisioning وحساب App Store Connect. هذه البيانات غير متاحة في المشروع ولا تُحفظ في Git. تستخدم GitHub Secrets عند تفعيل هذه الخطوة؛ لا ترسل كلمات المرور أو المفاتيح الخاصة في المحادثة.

## الحالة الحالية

أُعيد المشروع ووُصل ببناء اللعبة الحالية، وثبتت هوية التطبيق المنشور وفريقه. لم يُنشأ IPA موقّع ولم يُرسل تحديث إلى Apple. فئة الرأس/حساس الحركة وبقية ميزات المنصة تحتاج اختبارًا داخل WKWebView؛ لا يُدَّعى تكافؤ كامل مع Safari أو نسخة Replit من نجاح الترجمة وحده. تبقى تجربة التحديث فوق إصدار المتجر ضرورية للتحقق من المحفوظات والميزات.

المراجع: [رفع بناء إلى Apple](https://developer.apple.com/help/app-store-connect/manage-builds/upload-builds/)، [إنشاء إصدار جديد](https://developer.apple.com/help/app-store-connect/update-your-app/create-a-new-version/)، [متطلبات SDK الحالية](https://developer.apple.com/news/upcoming-requirements/)، [التوقيع على GitHub Actions](https://docs.github.com/en/actions/how-tos/deploy/deploy-to-third-party-platforms/sign-xcode-applications).
