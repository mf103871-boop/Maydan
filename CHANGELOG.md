## غير مُصدَر

### الحسابات و«ميدان بلس»

- اشتراك واحد «ميدان بلس» (شهري/سنوي، الأسعار من المتجر لا من الكود): بَديهة تبقي
  **أول عشر حزم مجانية** بترتيب البنك (`FREE_PACKS` في `src/shared/account/config.js`)
  والباقي مقفول بشارة «بلس» ومرشّح «مجاني» في المُنتقي؛ وكل لعبة أخرى تُلعب
  **مباراة كاملة واحدة مجانًا** (تُحسب عند `phase === 'over'` عبر `api.matchOver()`)
  ثم يفتح الجدار. إنشاء غرفة جماعية يُحسب مباراة (يرفض الخادم بـ`PLUS_REQUIRED` بعد
  التجربة للمسجَّل غير المشترك)، والدخول برمز مجاني دائمًا.
- حساب عبر Sign in with Apple / Google على الخادم نفسه (`server/accounts/`, D1 `DB`):
  جلسة مبهمة مُجزَّأة 180 يومًا بتدوير منزلق، `/api/me` مصدر الحقيقة للاستحقاقات
  والتجارب، تحقق مشتريات آبل بإعادة الجلب من App Store Server API، وwebhooks من
  Paddle بتوقيع HMAC ونافذة 5 دقائق ومنع التكرار. الويب يشتري عبر Paddle، وiOS عبر
  StoreKit (المرحلة التالية). مزوّد وهمي للتطوير فقط خلف `AUTH_DEV_FAKE=1`.
- الواجهة: `AccountProvider` + `useAccount()`، جدار «ميدان بلس» (سبب، مزايا، خطتان،
  دخول/اشتراك، استعادة على iOS، الشروط)، بطاقة الحساب في الإعدادات (دخول/خروج/إدارة/
  حذف الحساب)، شارات على بطاقات الرئيسية وملاحظة في تفاصيل اللعبة. «مسح البيانات»
  يبقي الحساب. بلا خادم مضبوط (`online.config.json` فارغ) تعمل الطبقة «دون اتصال»:
  لا قفل إضافي ولا دخول، والتجارب محلية فقط.
- الإعداد: `d1_databases` في `wrangler*.jsonc` (ضع `database_id` بعد
  `wrangler d1 create maydan-accounts`)، ترحيلات في `server/migrations/`
  (`npm run db:migrate`)، الأسرار عبر `wrangler secret put`، CSP تسمح بـPaddle.
  التفاصيل والتشغيل في `docs/ACCOUNTS.md`.

### الهوية البصرية
- الرسوم الصلصالية بقناة ألفا حقيقية (`scripts/art-alpha.mjs`, `npm run art:alpha`) بدل `mix-blend-mode` والقص الدائري؛ ظلّ صورة ثابت + ظلّ تلامس على الحامل، وحواف صلصالية موحّدة تنضغط عند الضغط.
- مكتبة حركة مشتركة في `ui.css`/`brand.css` (‎ui-*‎ و‎clay-*‎) تغطي المنصة والألعاب الست وغرف الإنترنت؛ ستار ملوّن عند بدء اللعبة، منصة تتويج متسلسلة، أختام صح/خطأ، تحذير المؤقت بحلقة وتظليل أحمر. كلها تحترم «تقليل الحركة».
- شاشة البداية بلا خط فاصل: تدرّج سماء يطابق لون الصورة ‎#41B9FA‎، غيوم وبالونات ونجوم CSS.

### بَديهة

- بعد كشف الإجابة يستطيع المضيف إخفاءها بزر «إخفاء الإجابة» (وإظهارها ثانيةً)؛
  تُخفى الإجابة والوسائط المكشوفة والتلميحات معًا، وتبقى لوحة الحسم ظاهرة،
  وتُحفظ الحالة مع المباراة الجارية.

### الأدوات والوثائق ومخزن العامل الخدمي

- `bank:validate`: قاعدة تسريب الإجابة صارت تسري على كل سؤال يحمل إجابة مكتوبة.
  كانت تتخطى كل سؤال له `type` — 4,320 من 18,960 سؤالًا (22.8%) — والاستثناء
  الباقي هو `order` و`odd` و`grid` وحدها لأن إجابتها داخل عناصرها.
- `bank:validate`: فحص جديد للحشو المُولَّد آليًا (قوالب «المسمى الداخلي»
  و«المعدود» و«… تأكيدًا لا …» و«الكلمة الجامعة رقم N» و«في شريحة N»، وسلاسل
  «اسم + عدّاد» داخل الفئة). يرسب الأمر عليه، ويطبع `npm run bank:placeholders`
  القائمة كاملة بالمعرّفات. عند إضافته كان في البنك **3,674 سؤالًا في 27 فئة**،
  كلها `verified: true` داخل فئات `done`؛ أُعيدت كتابة تلك الفئات كلها في هذه
  الدفعة (آخرها `spotdiff` و`sound` في 2026-09-15) فصار البنك **صفر حشو**، والاختبار
  يثبّت ذلك.
- **`خمن الصوت` (`sound`) أُعيد بناؤها**: كانت 188 من 240 نغمات مُولَّدة بأسئلة
  «ما هذا الصوت رقم N؟». صارت 240 تسجيلًا حقيقيًا (104 من أرشيف الإنترنت، 71 من
  Freesound عبر Openverse، 65 من كومنز) بطول 4–5 ثوانٍ و24–32 ك.ب/ث تحت سقف 4 م.ب
  للفئة، مع أسئلة تلميحية وبدائل إجابة (`docs/bank/review-sound-rebuilt-2026-09-15.md`).
- `bank:validate -- <id>` صار يطبّق القواعد العابرة للبنك (تكرار `qid`، تكرار نص
  السؤال، الإجابة المشتركة، سلامة `index.js`، فئة `done` بلا ملف) مقابل البنك
  كله بدل تخطّيها صامتًا، ويعلن نطاقه في المخرجات.
- `bank:status -- <id>` بلا `done|pending` صار يعرض حالة الفئة وأعدادها بدل
  طباعة سطر الاستعمال والخروج بخطأ.
- `npm run e2e:media` و`npm run e2e:offline` كانا يفشلان دائمًا: يختاران أول ست
  بطاقات في شاشة الفئات بينما الحزم التجريبية تظهر في آخرها (`order: 9000+`)،
  فتُلعب جولة نصية بلا وسائط. صارا يختارانها بالاسم ويتحققان من ذلك قبل اللعب.
- `public/sw.js`: حُذف المدخل المكرر `'./'`؛ كان المستند (6.9 م.ب) يُطلب ثلاث
  مرات في أول زيارة ويُخزَّن مرتين. صار طلبين ومخزنًا واحدًا: 14.37 → 7.66 م.ب.
- `public/_headers`: ترويسات الأمان (CSP و`nosniff` و`X-Frame-Options`
  و`Referrer-Policy`) للملفات الثابتة، لأن `assets.run_worker_first` يقصر تشغيل
  العامل على `/api` و`/health` فلا تمرّ بها الصفحة ولا الوسائط.
- `npm run release`: أرشيف المصدر صار يبني قائمته من `git ls-files`، فلم يعد
  يحمل `.wrangler/` و`.dev.vars` و`.env*` و`ios/Maydan/www/` رغم استثنائها في
  `.gitignore`.
- مسار `/home/user/-/.cache/fill-spec.json` من جهاز كاتب سابق أُزيل من مراحل
  Fill في `scripts/bank/workflows/*`.
- حُذف سير عملين ميتين: `.github/workflows/unpack-bank.yml`
  و`.github/workflows/prepare-cloudflare.yml` (أرشيفاتهما لم تعد موجودة، وكلاهما
  يفشل عند أي تشغيل ويحمل `contents: write`).
- تصحيح الوثائق: البنك 79 فئة و11 فئة وسائط و18,960 سؤالًا (لا 80 و12)،
  و`childhood` محذوفة لا معلّقة، وعناوين `docs/ONLINE.md` و`docs/CLOUDFLARE.md`
  على 1.4.1، والنشر من فرع `main` في `UPDATE-MAYDAN.md` كما في بقية الوثائق،
  وإزالة تعليمة رفع رقم `CACHE` يدويًا من README (البناء يضع البصمة)، والإشارة
  إلى `smoke.js` المحذوف في `docs/PLATFORM_REPORT.md`.
- اختباران: `tests/sw-release-regression.test.js` (جديد) و`tests/bank-pipeline.test.js`.
- **سُحبت فئة `أربع صور وكلمة` (`fourpics`)** بقرار صاحب المشروع: صورها الأربع
  كانت تُسحب من مجمّع 144 صورة مشترك لا يخصّ الإجابة، وتجربة إعادة البناء (12
  سؤالًا) أثبتت أن الاستكمال يحتاج نحو خمس ساعات وإعادة تصميم 96 إجابة.
  البنك 78 فئة و10 وسائط و18,720 سؤالًا؛ معرّفاتها في `docs/bank/retired-qids.json`
  (`docs/bank/review-fourpics-removed-2026-09-15.md`).

## 1.4.1 — 2026-09-13 — صور اللاعبين أثناء اللعب

- إصلاح قصّ صور الشخصيات في جداول النقاط ومنصة الفائزين واختيار اللاعبين؛ ألوان الخلفية تحافظ الآن على حجم صورة الشخصيات وطريقة عرضها.

## 1.4.0 — 2026-09-13 — فبركة من كل هاتف

- غرف فبركة لـ3–8 لاعبين بالرمز والرابط وQR، بجانب غرف مين فينا وعلى Worker نفسه.
- الأنماط الأربعة: حقائق، صور، مزيج، وأصحابنا؛ كتابة متزامنة، نقاش وتصويت سري، وكشف تدريجي موحد للجميع.
- حقيقة خاصة في دور الأصحاب، إجابات وأصوات مثبتة، معرّفات خيارات عشوائية، وعدم إرسال أسماء الكتّاب أو الحقيقة قبل الكشف.
- نفس قواعد النقاط والمساعدة ودمج الإجابات والمضاعفة الأخيرة، مع حصيلة وألقاب وأفضل فبركات للجلسة.
- حفظ حالة المباراة على الخادم واستعادة الاتصال وإسبات Durable Objects؛ المؤقتات تستمر عند إخفاء صفحة الهاتف.
- مسار مباشر `#/online/fabraka` واختيار اللعبة في شاشة الغرف، مع بقاء وضع الجهاز الواحد.
- اختبارات للسرية والنقاط والمهل والواجهات ومباريات متعددة الاتصالات على محرك Cloudflare وSQLite، دون ترحيل قواعد بيانات أو خدمات إضافية.

## 1.3.1 — 2026-09-12 — نشر كامل على Cloudflare

- إعداد اختياري لنشر اللعبة والوسائط والغرف على رابط Cloudflare واحد.
- ربط عنوان API تلقائيًا بعنوان الموقع في بناء Cloudflare.
- استثناء طلبات الغرف من التخزين المؤقت في PWA، واختبارات لتوجيه API قبل صفحة التطبيق.
- دليل وأوامر نشر كاملة دون الاعتماد على إضافة ChatGPT أو تحديث GitHub.
- تجهيز فرع `cloudflare-ready` للربط من الهاتف؛ إعداد Wrangler الافتراضي يشمل الموقع والغرف، والإعداد المنفصل في `wrangler.rooms.jsonc`.

## 1.3.0 — 2026-09-12 — تجربة الغرف

- غرف «مين فينا؟» لـ3–12 لاعبًا من هواتف مختلفة، ودعوة برمز من 6 أرقام أو رابط أو QR.
- اختيار الاسم والشخصية، غرفة انتظار وجاهزية، و5 أو 8 أو 12 جولة.
- تصويت سري لمدة 30 ثانية، واحتساب خادمي للنقاط والتعادل، ونتائج موحّدة.
- حفظ الجلسة والعودة بعد الانقطاع، مع انتقال الإدارة بعد غياب المضيف 20 ثانية.
- خادم Cloudflare Workers مع SQLite Durable Objects وWebSocket Hibernation، وإعداد للخطة المجانية.
- خادم محلي واختبارات اتصال متعددة اللاعبين ودليل تفعيل؛ بقية الألعاب تحتفظ بوضع الجهاز الواحد.

## 1.2.0 — 2026-09-12

- تطبيق الهوية الصلصالية المعتمدة على المنصة والألعاب الست.
- شخصيات وأيقونات ألعاب وكأس موحّدة وخلفية بداية عمودية.
- أزرار واضحة وخط عربي محلي، مع تضمين الرسوم والخط للّعب دون إنترنت.
- تحديث لوحة بَديهة والأسئلة والنتائج، مع بقاء البنك والقواعد والحفظ كما هي.
- تجهيز أيقونات الويب وPWA وiOS وخلفية الإطلاق العاجية.

# سجل التغييرات

## غير مُصدَر

### فبركة 2 — جلسة أكثر تفاعلًا

- أربعة أنماط: حقائق، صور أدوات، مزيج، وأسئلة عن الأصحاب بأدوار متساوية.
- عرض السؤال للجميع، ثم كتابة سرية بمؤقّت اختياري 30/45 ثانية، ومساعدة واحدة
  لكل لاعب، ونقاش بلا أسماء لمدة 15/20 ثانية قبل التصويت السري.
- كشف على دفعات، يجمع الحقيقة وأقوى كذبة في النهاية، مع تصويت ضحك مستقل،
  جائزة للجولة، وألقاب وإحصاءات وأفضل الفبركات للجلسة. مضاعفة أخيرة اختيارية.
- تثبيت الإجابات دون كشف الحقيقة أثناء الكتابة، وتوحيد الأرقام العربية والمكتوبة
  والصيغ البديلة، ودمج الإجابات المتطابقة، وتصحيح احتساب النقاط عند كتابة الحقيقة.
- إزالة 11 تكرارًا معنويًا وإضافة 8 غرائب موثقة: 149 سؤالًا، منها 79 مختارة؛
  إضافة 12 رسمًا أصليًا لأدوات و24 سؤال أصحاب، مع اختيار المواضيع.
- حفظ التقدم والمسودات والمؤقّت محليًا، واستئناف خلف شاشة تسليم مغلقة، وحفظ
  الأسئلة المعروضة فورًا. رسالة صريحة إذا تعذر الحفظ، وإيقاف عند إخفاء الصفحة
  أو طلب الخروج. لا تحتاج الأنماط الجديدة ملفات أو خدمات خارجية.
- اختبارات موسّعة للقواعد والمحتوى والاستئناف ولعدم ظهور الإجابات السرية في
  الواجهة. التفاصيل في `docs/FABRAKA.md`.

### إعادة بناء بنك «بَديهة» — البنية التحتية

- **حُذف بنك الأسئلة كاملًا** (43 فئة، 1,032 سؤالًا) بقرار من صاحب اللعبة؛ يُعاد
  بناؤه فئةً فئة من 80 فئة اختارها، بـ48 سؤالًا في كل شريحة (240 لكل فئة).
- `docs/bank/RUBRIC.md` (قواعد الصعوبة والأسلوب والدراما والوسائط والمحتوى)
  و`docs/bank/SCHEMA.md` (كيف تختار اللعبة الأسئلة، وبنية الملفات والحقول).
- `src/data/bank-status.json`: حالة كل فئة وأعدادها وحدودها، ويقرؤه المدقّق والاختبارات.
- أوامر: `bank:validate` (يرسب على أي مخالفة)، `bank:report`، `bank:sort` (يرتّب
  ويعيد توليد index.js)، `bank:status`، و`media:fetch` (بحث في Commons وOpenverse
  وNASA وArchive بفلتر ترخيص، تنزيل، معالجة sharp/ffmpeg، وتسجيل الإسناد).
- كائن الإسناد في `media` ({ src, type, title, sourceUrl, author, license, licenseUrl })،
  وشاشة «المصادر والتراخيص» في «حول» تعرضه لكل ملف.
- أنواع عرض جديدة: `image` بمؤثر `jumble` (ركّبها صح)، أربع صور في سؤال، `choice`
  (قبل ولا بعد وأمثاله)، و`code` (الشفرة). الصورة تُكبَّر باللمس، ومقطع الصوت
  يُسمع ثلاث مرات قبل الكشف.
- إصلاحان من تقرير التحقق: أرشيف الإصدار كان يحدّث أرشيفًا قديمًا فيعيد نشر
  ملفات محذوفة (صار يُنشأ من جديد، ويرفض الإصدار أصلًا إن كانت الحزم التجريبية
  مركّبة)، ومفتاح مخزن العامل الخدمي كان نصًّا مجمّدًا (صار الإصدار + بصمة
  المستند، فيصل التحديث للمثبّتين من أول فتحة).

### ما كشفته مراجعة الحزمة 0

- **اصطدام أصناف CSS**: خيارات سؤال «قبل ولا بعد» كانت تُسمّى `.m-choice` وهو صنف
  أزرار اختيار النمط في شاشة الإعداد، فتكسر تخطيطها. صارت `.m-options`.
- **مكوّنات الوسائط نُقلت خارج مكوّن اللعبة**: تعريفها بالداخل كان يعيد تركيبها مع
  كل دقّة مؤقت، فيتوقف الصوت وتعود قطع «إكشفني» إلى الغطاء بعد ثانية.
- **صندوق خطأ لا يزول**: ملف أخفق في جولة بلا شبكة ثم نجح لاحقًا كان يبقى معطوبًا
  في العرض؛ صار المحمَّل يسبق الفاشل، وزر الإعادة ينجح بانعدام الإخفاقات.
- **تطبيع عربي واحد** لكل المشروع (`arabicNormalize`): كان المدقّق يقبل خيارًا لا
  تستطيع الشاشة إبرازه لاختلاف علامات الترقيم بينهما.
- **عدّ مرات الصوت** صار عند بدء التشغيل فعلًا لا عند الضغط، فلا تُحرق مرة على
  مقطع ما زال يُحمَّل.
- **تسريب إجابة**: الفحص صار مطابقة كلمة كاملة مع السوابق (و/ب/ال…) بدل مقطع، فلا
  تُحسب «مصر» داخل «مصرف». وأنماط أسئلة الصنّاع صارت بصيغ السؤال لا بالكلمة
  المفردة، فلا يُرفض «ما اسم المخرج السري الذي هرب منه أبو عصام؟».
- **صح/خطأ والاختيار** أُعفيا من فحص تكرار الإجابة (إجاباتهما من مجموعة مغلقة).
- **`?v=` لكل مجلد وسائط**: بصمة محتوى المجلد تُلحق بالعناوين، فملف يُصحَّح بالاسم
  نفسه يصل للاعب بدل أن يعلق على النسخة المخزّنة.
- **`.gitignore`**: النمط `demo*.json` كان سيُخفي الفئة الحقيقية `demonslayer`؛ صار
  `demo[a-f].json`.
- **الصورة المكبَّرة** لا تكشف الإجابة في نصّها البديل قبل الكشف، و«ركّبها صح» يأخذ
  نسبة الصورة الحقيقية بدل مطّها في إطار 4:3، وأربع صور لا تقبل مؤثرًا.
- **اختبارات من طرف إلى طرف** في `scripts/e2e/` (`npm run e2e:media` و
  `npm run e2e:offline`) تشغّل كل نوع سؤال في Chromium وتلعب جولة بلا شبكة.

### وسائط «بَديهة»

- تسعة أنواع أسئلة جديدة تعمل بملفات حقيقية أو بلا ملفات: `image` (بمؤثرات
  `zoom` و`blur` و`silhouette` و`reveal`)، `audio`، `video`، `diff`،
  `truefalse`، `scramble`، `complete`، `common`، و`hints`.
- `hints` يخصم 25% من نقاط السؤال عن كل تلميح يُطلب، بحدّ أدنى الربع.
- ملفات السؤال تُكتب باسم قصير ويكملها مجلد الحزمة (`media/<pack>/01.webp`)،
  ويجوز أن تكون عنوانًا خارجيًا كاملًا.
- تحميل مسبق في الخلفية عند بناء اللوحة، فلا ينتظر اللاعب عند فتح السؤال.
- الملف الذي يتعذّر تحميله يعرض سبب العطل وزر «أعد المحاولة» بدل مربع فارغ.
- صف «وسائط الفئات المختارة» في شاشة الإعداد يُنزّل ملفات الجولة دفعة واحدة
  ويخزّنها في العامل الخدمي، فتُلعب بلا اتصال. لا يظهر للحزم النصية.
- مخزن وسائط مستقل (`maydan-media-v1`) لا يُمسح عند ترقية اللعبة.
- `npm run build` **يفشل** إن أشار سؤال إلى ملف غير موجود أو إلى امتداد لا
  يطابق نوعه، ويطبع حجم وسائط كل حزمة.
- `docs/PACK_FORMAT.md`: مرجع كامل لصيغة الحزم وكل أنواع الأسئلة.
- `node scripts/demo-packs.mjs` يركّب ست حزم تجريبية تغطي كل الأنواع لتجربة
  المسار محليًا، و`--remove` يزيلها. مخرجاتها لا تُنشر.

### تصحيح الوصف

- «تعمل دون إنترنت» صار أدقّ: اللعبة نفسها كذلك، أما حزم الوسائط فتحتاج الشبكة
  أول مرة ثم تُحفظ في الجهاز.

## 1.0.0

أول إصدار من **منصة ميدان**: لعبة الأسئلة الواحدة صارت منصة تضم ست ألعاب.

### المنصة

- شاشة افتتاح، وشاشة رئيسية ببطاقات الألعاب، وشاشة تفاصيل لكل لعبة، وإعدادات، وصفحة تعريف.
- **دفتر اللاعبين**: أسماء وإيموجي وألوان تُحفظ مرة وتُستخدم في كل الألعاب الفردية (حتى 12 لاعبًا).
- موجّه على الـhash مع زر رجوع وتأكيد خروج أثناء اللعب.
- نظام تصميم واحد في `tokens.css`، ومكونات مشتركة (أزرار، بطاقات، نوافذ، ورقة سفلية، تنبيهات، مؤقت، لوحة نتائج، منصة تتويج).
- مؤثرات: صوت مُصنَّع بـ WebAudio، اهتزاز، قصاصات على canvas، انتقالات شاشات، ودخول متدرج للبطاقات — كلها تحترم «تقليل الحركة».
- Wake Lock يُبقي الشاشة مضاءة أثناء اللعب، والمؤقت يتوقف عند إخفاء الصفحة ويستأنف بعد 3-2-1.
- تطبيق قابل للتثبيت (PWA) يعمل دون إنترنت.

### الألعاب

- **بَديهة** (كانت «ميدان»): منقولة كما هي بكل ميزاتها — فرق 2–4، ست فئات، شرائح 200–1000، أوضاع عائلي/تحدٍّ/خبراء، جولات 20/30/60، مؤقت، وسائل مساعدة، سرقات، حفظ واستئناف، وإبلاغ عن الأسئلة.
- **قبل ما يطق!** — جديدة: «3 قبل الصفارة» بالنقاط، و«القنبلة» بمؤقت مخفي وأرواح.
- **ممنوع!** — جديدة: وصف الكلمة دون خمس كلمات ممنوعة، وزر «ممنوع!» للخصم.
- **على جبينك** — جديدة: تخمين بالإمالة (مع إذن مستشعر iOS) أو باللمس، ومراجعة يدوية بعد الجولة.
- **فَبْرَكة** — جديدة: كتابة سرية لإجابات مزيفة، تصويت، وكشف تدريجي.
- **مين فينا؟** — جديدة: وضع الإشارة الجماعي أو التصويت السري، وألقاب في النهاية.

### التحديث على الأجهزة

- من فتح الرابط قبل هذا الإصدار كانت نسخته القديمة مخزّنة على جهازه، وكان يرى اللعبة القديمة مرة قبل أن تظهر المنصة. صار العامل الخدمي يعيد تحميل الصفحة عند ترقية المخزن، فيصل التحديث من أول فتحة.

### الترحيل

- مفاتيح التخزين صارت تحت `maydan:<gameId>:`. تُرحَّل مفاتيح «ميدان» القديمة تلقائيًا عند أول تشغيل، فلا تضيع مباراة محفوظة ولا سجل نتائج.
