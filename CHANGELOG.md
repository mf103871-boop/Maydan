# سجل التغييرات

## غير مُصدَر

### إعادة بناء بنك «بَديهة» — البنية التحتية

- **حُذف بنك الأسئلة كاملًا** (43 فئة، 1,032 سؤالًا) بقرار من صاحب اللعبة؛ يُعاد
  بناؤه فئةً فئة من 80 فئة اختارها، بـ48 سؤالًا في كل شريحة (240 لكل فئة).
- `docs/bank/RUBRIC.md` (قواعد الصعوبة والأسلوب والدراما والوسائط والمحتوى)
  و`docs/bank/SCHEMA.md` (كيف تختار اللعبة الأسئلة، وبنية الملفات والحقول).
- `src/data/bank-status.json`: حالة كل فئة وأعدادها وحدودها، ويقرؤه المدقّق والاختبارات.
- أوامر: `bank:validate` (يرسب على أي مخالفة)، `bank:report`، `bank:sort` (يرتّب
  ويعيد توليد index.js)، `bank:status`، و`media:fetch` (بحث في Commons وOpenverse
  وNASA وArchive بفلتر ترخيص، تنزيل، معالجة sharp/ffmpeg، وتسجيل الإسناد).
- كائن الإسناد في `media` ({ src, type, title, sourceUrl, author, license, licenseUrl })،
  وشاشة «المصادر والتراخيص» في «حول» تعرضه لكل ملف.
- أنواع عرض جديدة: `image` بمؤثر `jumble` (ركّبها صح)، أربع صور في سؤال، `choice`
  (قبل ولا بعد وأمثاله)، و`code` (الشفرة). الصورة تُكبَّر باللمس، ومقطع الصوت
  يُسمع ثلاث مرات قبل الكشف.
- إصلاحان من تقرير التحقق: أرشيف الإصدار كان يحدّث أرشيفًا قديمًا فيعيد نشر
  ملفات محذوفة (صار يُنشأ من جديد، ويرفض الإصدار أصلًا إن كانت الحزم التجريبية
  مركّبة)، ومفتاح مخزن العامل الخدمي كان نصًّا مجمّدًا (صار الإصدار + بصمة
  المستند، فيصل التحديث للمثبّتين من أول فتحة).

### ما كشفته مراجعة الحزمة 0

- **اصطدام أصناف CSS**: خيارات سؤال «قبل ولا بعد» كانت تُسمّى `.m-choice` وهو صنف
  أزرار اختيار النمط في شاشة الإعداد، فتكسر تخطيطها. صارت `.m-options`.
- **مكوّنات الوسائط نُقلت خارج مكوّن اللعبة**: تعريفها بالداخل كان يعيد تركيبها مع
  كل دقّة مؤقت، فيتوقف الصوت وتعود قطع «إكشفني» إلى الغطاء بعد ثانية.
- **صندوق خطأ لا يزول**: ملف أخفق في جولة بلا شبكة ثم نجح لاحقًا كان يبقى معطوبًا
  في العرض؛ صار المحمَّل يسبق الفاشل، وزر الإعادة ينجح بانعدام الإخفاقات.
- **تطبيع عربي واحد** لكل المشروع (`arabicNormalize`): كان المدقّق يقبل خيارًا لا
  تستطيع الشاشة إبرازه لاختلاف علامات الترقيم بينهما.
- **عدّ مرات الصوت** صار عند بدء التشغيل فعلًا لا عند الضغط، فلا تُحرق مرة على
  مقطع ما زال يُحمَّل.
- **تسريب إجابة**: الفحص صار مطابقة كلمة كاملة مع السوابق (و/ب/ال…) بدل مقطع، فلا
  تُحسب «مصر» داخل «مصرف». وأنماط أسئلة الصنّاع صارت بصيغ السؤال لا بالكلمة
  المفردة، فلا يُرفض «ما اسم المخرج السري الذي هرب منه أبو عصام؟».
- **صح/خطأ والاختيار** أُعفيا من فحص تكرار الإجابة (إجاباتهما من مجموعة مغلقة).
- **`?v=` لكل مجلد وسائط**: بصمة محتوى المجلد تُلحق بالعناوين، فملف يُصحَّح بالاسم
  نفسه يصل للاعب بدل أن يعلق على النسخة المخزّنة.
- **`.gitignore`**: النمط `demo*.json` كان سيُخفي الفئة الحقيقية `demonslayer`؛ صار
  `demo[a-f].json`.
- **الصورة المكبَّرة** لا تكشف الإجابة في نصّها البديل قبل الكشف، و«ركّبها صح» يأخذ
  نسبة الصورة الحقيقية بدل مطّها في إطار 4:3، وأربع صور لا تقبل مؤثرًا.
- **اختبارات من طرف إلى طرف** في `scripts/e2e/` (`npm run e2e:media` و
  `npm run e2e:offline`) تشغّل كل نوع سؤال في Chromium وتلعب جولة بلا شبكة.

### وسائط «بَديهة»

- تسعة أنواع أسئلة جديدة تعمل بملفات حقيقية أو بلا ملفات: `image` (بمؤثرات
  `zoom` و`blur` و`silhouette` و`reveal`)، `audio`، `video`، `diff`،
  `truefalse`، `scramble`، `complete`، `common`، و`hints`.
- `hints` يخصم 25% من نقاط السؤال عن كل تلميح يُطلب، بحدّ أدنى الربع.
- ملفات السؤال تُكتب باسم قصير ويكملها مجلد الحزمة (`media/<pack>/01.webp`)،
  ويجوز أن تكون عنوانًا خارجيًا كاملًا.
- تحميل مسبق في الخلفية عند بناء اللوحة، فلا ينتظر اللاعب عند فتح السؤال.
- الملف الذي يتعذّر تحميله يعرض سبب العطل وزر «أعد المحاولة» بدل مربع فارغ.
- صف «وسائط الفئات المختارة» في شاشة الإعداد يُنزّل ملفات الجولة دفعة واحدة
  ويخزّنها في العامل الخدمي، فتُلعب بلا اتصال. لا يظهر للحزم النصية.
- مخزن وسائط مستقل (`maydan-media-v1`) لا يُمسح عند ترقية اللعبة.
- `npm run build` **يفشل** إن أشار سؤال إلى ملف غير موجود أو إلى امتداد لا
  يطابق نوعه، ويطبع حجم وسائط كل حزمة.
- `docs/PACK_FORMAT.md`: مرجع كامل لصيغة الحزم وكل أنواع الأسئلة.
- `node scripts/demo-packs.mjs` يركّب ست حزم تجريبية تغطي كل الأنواع لتجربة
  المسار محليًا، و`--remove` يزيلها. مخرجاتها لا تُنشر.

### تصحيح الوصف

- «تعمل دون إنترنت» صار أدقّ: اللعبة نفسها كذلك، أما حزم الوسائط فتحتاج الشبكة
  أول مرة ثم تُحفظ في الجهاز.

## 1.0.0

أول إصدار من **منصة ميدان**: لعبة الأسئلة الواحدة صارت منصة تضم ست ألعاب.

### المنصة

- شاشة افتتاح، وشاشة رئيسية ببطاقات الألعاب، وشاشة تفاصيل لكل لعبة، وإعدادات، وصفحة تعريف.
- **دفتر اللاعبين**: أسماء وإيموجي وألوان تُحفظ مرة وتُستخدم في كل الألعاب الفردية (حتى 12 لاعبًا).
- موجّه على الـhash مع زر رجوع وتأكيد خروج أثناء اللعب.
- نظام تصميم واحد في `tokens.css`، ومكونات مشتركة (أزرار، بطاقات، نوافذ، ورقة سفلية، تنبيهات، مؤقت، لوحة نتائج، منصة تتويج).
- مؤثرات: صوت مُصنَّع بـ WebAudio، اهتزاز، قصاصات على canvas، انتقالات شاشات، ودخول متدرج للبطاقات — كلها تحترم «تقليل الحركة».
- Wake Lock يُبقي الشاشة مضاءة أثناء اللعب، والمؤقت يتوقف عند إخفاء الصفحة ويستأنف بعد 3-2-1.
- تطبيق قابل للتثبيت (PWA) يعمل دون إنترنت.

### الألعاب

- **بَديهة** (كانت «ميدان»): منقولة كما هي بكل ميزاتها — فرق 2–4، ست فئات، شرائح 200–1000، أوضاع عائلي/تحدٍّ/خبراء، جولات 20/30/60، مؤقت، وسائل مساعدة، سرقات، حفظ واستئناف، وإبلاغ عن الأسئلة.
- **قبل ما يطق!** — جديدة: «3 قبل الصفارة» بالنقاط، و«القنبلة» بمؤقت مخفي وأرواح.
- **ممنوع!** — جديدة: وصف الكلمة دون خمس كلمات ممنوعة، وزر «ممنوع!» للخصم.
- **على جبينك** — جديدة: تخمين بالإمالة (مع إذن مستشعر iOS) أو باللمس، ومراجعة يدوية بعد الجولة.
- **فَبْرَكة** — جديدة: كتابة سرية لإجابات مزيفة، تصويت، وكشف تدريجي.
- **مين فينا؟** — جديدة: وضع الإشارة الجماعي أو التصويت السري، وألقاب في النهاية.

### التحديث على الأجهزة

- من فتح الرابط قبل هذا الإصدار كانت نسخته القديمة مخزّنة على جهازه، وكان يرى اللعبة القديمة مرة قبل أن تظهر المنصة. صار العامل الخدمي يعيد تحميل الصفحة عند ترقية المخزن، فيصل التحديث من أول فتحة.

### الترحيل

- مفاتيح التخزين صارت تحت `maydan:<gameId>:`. تُرحَّل مفاتيح «ميدان» القديمة تلقائيًا عند أول تشغيل، فلا تضيع مباراة محفوظة ولا سجل نتائج.
