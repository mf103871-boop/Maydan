{
  "name": "maydan-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "منصة ميدان — ألعاب جماعية على جهاز واحد للأصدقاء والعائلة",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test \"tests/**/*.test.js\"",
    "release": "node scripts/release.mjs"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "react": "19.2.5",
    "react-dom": "19.2.5"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
